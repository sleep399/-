# 沙盘摄像头视频流获取

> 来源文件: `沙盘摄像头视频流获取.pdf`

## 第 1 页

如果想测试自己的模型在沙盘好不好用,可以通过下面的方式获取沙盘摄像头的视频流:
1 rtsp://10.126.59.120:8554/live/live1 # 桥面
2
3 rtsp://10.126.59.120:8554/live/live2 # 停车场出口
4
5 rtsp://10.126.59.120:8554/live/live3 # 行人检测
6
7 rtsp://10.126.59.120:8554/live/live4 #消防车识别
8
9 rtsp://10.126.59.120:8554/live/live5 # 桥出口
10
11 rtsp://10.126.59.120:8554/live/live6 # 桥入口
12
13 rtsp://10.126.59.120:8554/live/live7 # 道路2
14
15 rtsp://10.126.59.120:8554/live/live8 # 隧道(事故识别)
16
17 rtsp://10.126.59.120:8554/live/live9 # 隧道(车辆数量)
18
19 rtsp://10.126.59.120:8554/live/live10 # 道路3
20
21 rtsp://10.126.59.120:8554/live/live11 # 停车场入口
22
23 rtsp://10.126.59.120:8554/live/live12 # 道路1
读取并显示摄像头数据参考代码如下:
1 import cv2
2
3
4 def main():
5 rtsp_url = "rtsp://10.126.59.120:8554/live/live1"
6
7 # 使用 FFMPEG 后端并开启 TCP 传输,减少花屏/延迟
8 cap = cv2.VideoCapture(rtsp_url, cv2.CAP_FFMPEG)
9 cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)
10
11 if not cap.isOpened():
12 print(f"无法打开视频流: {rtsp_url}")
13 return
14
15 print(f"成功连接到视频流: {rtsp_url}")
16 print("按 q 退出,按 s 截图保存为 snapshot.jpg")
17
18 while True:
19 ret, frame = cap.read()
20 if not ret:
21 print("读取帧失败,流可能已断开")
22 break
23
24 cv2.imshow("RTSP Stream", frame)
25
26 key = cv2.waitKey(1) & 0xFF
27 if key == ord("q"):
28 break
29 elif key == ord("s"):

## 第 2 页

30 cv2.imwrite("snapshot.jpg", frame)
31 print("已保存截图: snapshot.jpg")
32
33 cap.release()
34 cv2.destroyAllWindows()
35
36
37 if __name__ == "__main__":
38 main()
识别并显示识别后的画面参考代码(以yolo为例):
1 import cv2
2 from ultralytics import YOLO
3
4
5 def main():
6 rtsp_url = "rtsp://10.126.59.120:8554/live/live1"
7
8 # 加载 YOLOv8 预训练模型(替换成自己的模型)
9 model = YOLO("yolov8n.pt")
10
11 # 读取 RTSP 流
12 cap = cv2.VideoCapture(rtsp_url, cv2.CAP_FFMPEG)
13 cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)
14
15 if not cap.isOpened():
16 print(f"无法打开视频流: {rtsp_url}")
17 return
18
19 print(f"成功连接到视频流: {rtsp_url}")
20 print("按 q 退出")
21
22 while True:
23 ret, frame = cap.read()
24 if not ret:
25 print("读取帧失败,流可能已断开")
26 break
27
28 # YOLO 推理(stream=True 更省内存)
29 results = model(frame, verbose=False)
30
31 # 在原图上绘制识别框和标签
32 annotated = results[0].plot()
33
34 cv2.imshow("YOLO Detection", annotated)
35
36 if cv2.waitKey(1) & 0xFF == ord("q"):
37 break
38
39 cap.release()
40 cv2.destroyAllWindows()
41
42
43 if __name__ == "__main__":
44 main()

## 第 3 页

> 本页未提取到可选择文本。
